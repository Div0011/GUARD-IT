/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/GUARD-IT',
  assetPrefix: '/GUARD-IT/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
